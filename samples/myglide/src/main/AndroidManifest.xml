<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
   package="com.example.myglide">

   <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/Theme.Glideparent">
      <activity
         android:name=".GlideActivity"
         android:screenOrientation="landscape"
         android:theme="@style/FullScreenLight">
         <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
         </intent-filter>
      </activity>

      <activity
         android:name=".GlideBugActivity"
         android:screenOrientation="landscape"
         android:theme="@style/FullScreenLight" />

      <activity
         android:name=".GlideBugTVActivity"
         android:screenOrientation="landscape"
         android:theme="@style/FullScreenLight" />
      <activity
         android:name=".GlideFlowActivity"
         android:screenOrientation="landscape"
         android:theme="@style/FullScreenLight" />
   </application>

   <uses-permission android:name="android.permission.INTERNET" />
</manifest>